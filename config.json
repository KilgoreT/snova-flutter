[
  {
    "key": "collectionName",
    "type": "string",
    "default": "Semantic Color",
    "title": "Token collection to export",
    "description": "Only tokens from the collection with this name are exported. Defaults to the semantic color collection."
  },
  {
    "key": "generateDisclaimer",
    "type": "boolean",
    "default": true,
    "title": "Show generated-file disclaimer",
    "description": "When enabled, a comment noting that the file was generated automatically and should not be edited by hand is added to every generated file."
  },
  {
    "key": "basePath",
    "type": "string",
    "default": "./packages/ui_kit_litnet_audio/lib/sam",
    "title": "Base output directory",
    "description": "Base directory (relative to the export root) where generated files are placed. Per-token-type files go into sub-paths under it."
  },
  {
    "key": "colorPath",
    "type": "string",
    "default": "/colors",
    "title": "Colors sub-path",
    "description": "Sub-path under the base directory for the generated color file."
  },
  {
    "key": "colorFileName",
    "type": "string",
    "default": "app_colors",
    "title": "Color file name",
    "description": "File name (without .dart extension) for the generated color file."
  },
  {
    "key": "rootClassName",
    "type": "string",
    "default": "AppColors",
    "title": "Root class name",
    "description": "Name of the public root class. Access default-theme colors as RootClass.bg.brand.primary and any theme as RootClass.dark.bg...."
  },
  {
    "key": "defaultThemeName",
    "type": "string",
    "default": "Light",
    "title": "Default theme",
    "description": "Theme reachable directly on the root class without naming it (e.g. AppColors.bg == this theme). Other themes are reachable only via RootClass.<theme>."
  }
]
